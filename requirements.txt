# RTrees segmentation needs OpenCV's ml module, which ships in the contrib build.
opencv-contrib-python>=4.8
numpy>=1.24
# Pre-mortem §4 ¶2: hard-pin scipy>=1.12 even though EMD was dropped in
# the v2 constellation design — defense against future drift if EMD is
# reconsidered. phase3_constellation.py enforces this at import time.
scipy>=1.12
scikit-image>=0.19
pandas>=2.0
matplotlib>=3.7
# Phase 3.5 — slide QR/DataMatrix decoding. zxing-cpp is the primary
# decoder and cv2 (already required above) is the fallback.
zxing-cpp>=2.2
# Test runner.
pytest>=7.4
pytest-mock>=3.10
# Optional — used by QR tests to generate synthetic QR fixtures.
qrcode>=7.4
